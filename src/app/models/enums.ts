export enum FileType {
    Article = "article",
    Profile = "profile"
}

export enum FileStatus {
    InProgress = "In progress",
    Published = "Published",
    Scheduled = "Scheduled",
    Approved = "Approved",    
    ForReview = "For review"
}
